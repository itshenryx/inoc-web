/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  experimental: {
    appDir: true,
    // serverComponentsExternalPackages: ['cryptico'],
  },
}

module.exports = nextConfig
